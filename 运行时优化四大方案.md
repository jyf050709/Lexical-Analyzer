# ToyC 编译器：四个高收益运行时优化方案（面向 RISC-V32 后端）

本文档聚焦于**让生成的 RISC-V32 汇编跑得更快**（运行时优化），面向你当前的架构：**AST（含 Optimizer）→ 直接生成汇编**。

你当前已有的关键点：

- `-opt` 会在 AST 上做多轮优化（常量折叠/传播、DCE、CSE、循环不变量外提、强度削减、已知循环完全展开、尾递归优化、函数内联等）。
- 后端 `CodeGenerator::genExpr()` 已包含一部分“常量乘/除/模”的强度削减（例如 2 的幂转移位）。

但目前生成代码的热点开销主要仍来自：

- **大量 `lw/sw`**（表达式求值时频繁把中间值压栈/出栈；局部变量每次读取都从栈加载）
- **函数调用前后搬运参数**（先全部存栈再加载进 `a0-a7`，以及栈对齐未系统处理）
- **统一且偏保守的 prologue/epilogue**（叶函数仍保存/恢复 `ra`/`s0`，且栈帧固定大）
- **指令级冗余**（`mv x,x`、可合并的 `addi sp`、可简化的算术序列等）

下面给出四个在你代码结构中**最容易落地、收益最高**的优化方案，并说明实现策略、正确性边界、与现有代码的衔接点。

---

## 总览（建议优先级）

| 方案 | 主要收益 | 难度 | 主要改动位置（建议） |
|---|---|---:|---|
| 1) 表达式/局部寄存器化（减少 `lw/sw`） | 最高 | 高 | `CodeGenerator::genExpr()` / `genStmt()` / 每函数预分析 |
| 2) 调用序列优化 + 16B 对齐 | 高 | 中 | `CodeGenerator::genExpr()` 的 `CALL` 分支 |
| 3) 叶函数/栈帧优化 | 中-高 | 中 | `CodeGenerator::genFunc()` + 每函数预分析 |
| 4) 汇编窥孔优化（post-pass） | 中 | 低 | `CodeGenerator::generate()` 返回前后处理 |

四者的关系：

- (1) 会显著减少“表达式压栈/出栈”，也会让 (3) 的“无栈帧/小栈帧”更容易成立。
- (2) 和 (3) 直接影响函数边界/调用边界的开销；即使不做 (1)，也能明显见效。
- (4) 是“低风险补刀”，能把后端的保守输出再压一遍冗余。

---

## 1) 表达式树寄存器分配 + 局部变量寄存器化（减少 `lw/sw`）

### 1.1 现状瓶颈（为什么慢）

当前的二元表达式一般是：

1. 先算左子树到 `t0`
2. `addi sp, sp, -4; sw t0, 0(sp)` 把中间值压栈
3. 再算右子树到 `t0`，搬到 `t1`
4. `lw t0, 0(sp); addi sp, sp, 4` 取回左值
5. 做 `add/sub/mul/...`

这会导致**每个二元节点都引入至少 2 次内存访问 + 2 次 sp 调整**。表达式越深，越明显。

另一个热点是局部变量访问：`IdentExpr` 每次都 `lw`，即使变量在一个 basic block 内被反复使用也无法复用寄存器值。

### 1.2 表达式树寄存器分配：Sethi–Ullman（SU）编号

目标：在不引入完整 CFG/SSA 的前提下，把“表达式中间值”尽量留在寄存器里，减少压栈。

核心思想：

- 给每个表达式节点计算一个 **SU 需求值**：该子树在无溢出（spill）的情况下需要多少个寄存器。
- 用 SU 值决定求值顺序：**先算更“吃寄存器”的子树**，以减少溢出概率。

#### 1.2.1 SU 规则（适用于你的 AST）

记 `SU(e)` 为子表达式 `e` 的寄存器需求。

- `NUMBER` / `IDENT`：`SU = 1`
- `UNARY(op, x)`：`SU = SU(x)`
- `BINARY(op, l, r)`：
  - 若 `SU(l) == SU(r)`：`SU = SU(l) + 1`
  - 否则：`SU = max(SU(l), SU(r))`
- `CALL`：建议保守处理：
  - 若你暂时不做“跨 call 的活跃值保护”，可直接设 `SU = 大值`（迫使上层 spill）
  - 或者设 `SU = 1` 但在生成前强制把活跃寄存器 spill（见 1.2.3）

#### 1.2.2 选择可用寄存器集合

对 RISC-V32 常见选择：

- 临时寄存器：`t0-t6`（caller-saved），适合表达式临时值
- 需要更激进时可用 `a0-a7` 作为临时（但会与调用约定冲突；建议后期再做）

建议第一版只用 `t0-t6`（7 个寄存器）。当 SU 超过可用寄存器数时，才 spill 到栈。

#### 1.2.3 生成策略（伪代码）

给定一个寄存器池 `freeRegs = [t0..t6]`，实现：

1. `genExprReg(expr, targetReg)`：结果写入 `targetReg`
2. 对 `BINARY(l, r)`：
   - 若 `SU(l) > SU(r)`：先生成 `l`，再生成 `r`
   - 否则：先生成 `r`，再生成 `l`（或保持左优先以兼容既有语义，见下）
3. 子树生成时从 `freeRegs` 取寄存器；不够时 spill

对“语义兼容”的注意：

- 如果 ToyC 规定了**实参/表达式左到右求值**，你就不能随意交换 `l`/`r` 的求值顺序（尤其是包含函数调用的表达式）。
- 如果 ToyC 语义不要求顺序（类似 C 的未定义/未指定），你可以按 SU 自由选择顺序，收益更大。
- 折中方案：对无 `CALL` 的纯算术/比较表达式允许重排；一旦子树含 `CALL` 就固定左到右，并在 call 前 spill 活跃寄存器。

#### 1.2.4 与 `CALL` 的交互（最容易踩坑的点）

`call` 会破坏 caller-saved（`t*`、`a*`）寄存器的值。两种常见实现路线：

- **路线 A（保守，易实现）**：在生成 `CALL` 前，把当前所有“活跃临时寄存器”spill 到栈；call 后再 reload。实现简单但收益打折。
- **路线 B（分割表达式）**：把含 call 的表达式提前拆成临时变量（你已有 CSE/临时变量机制的雏形），再以语句级顺序生成，减少跨 call 的活跃寄存器。

建议先做路线 A，保证正确性，再逐步优化。

### 1.3 局部变量寄存器化（registerization）

目标：把“热点局部变量”长期放进寄存器，减少 `IdentExpr` 的 `lw` 和 `Assign` 的 `sw`。

#### 1.3.1 为什么在 ToyC 特别合适

ToyC 当前没有指针、取址、引用等特性（也没有显式的内存别名），因此：

- 局部变量不会被“未知指针写”悄悄改掉
- 只需处理 `Assign` 和 `VarDecl` 的显式写入即可

这让 registerization 的正确性约束大幅降低。

#### 1.3.2 一个可落地的“轻量分配器”

不引入全套 CFG/SSA 的情况下，可以做一个“每函数”的简单策略：

1. 预扫描函数体：统计每个局部变量的使用次数（读/写次数分开计更好）
2. 选择 Top-K 变量分配到寄存器
3. 优先使用 **callee-saved**（`s1-s11`），避免每次 `CALL` 前后保存
4. 在 `genFunc()` 的 prologue 保存被用到的 `s*`，epilogue 恢复

建议 K 从 2~6 开始（太多会让 prologue/epilogue 变长，反而拖慢）。

#### 1.3.3 与作用域/遮蔽（shadowing）的处理

你当前的变量环境是按 `varScopes`（逐层 `map<string,...>`）管理的，遮蔽是天然支持的。

寄存器化也建议沿用同样的作用域栈：

- 每层 scope 里记录 `name -> (stackOffset | regName)` 的绑定
- 进入新 scope 时 push；退出时 pop
- 内层同名变量会覆盖外层绑定，退出 scope 后自动恢复外层绑定

#### 1.3.4 与现有优化器的协同

你已有 `Optimizer` 的变量使用计数、死变量消除等能力。两种接入方式：

- **接入 A（推荐）**：在 `CodeGenerator` 内再做一遍“轻量 use count”，只针对 codegen 所需信息（更独立）
- **接入 B**：复用 `Optimizer` 统计结果并把选择结果写回 AST 或附加表（需要更多结构改造）

### 1.4 验证与用例

建议做两类测试：

1. **表达式压力**（验证栈压入/弹出明显减少）
   ```c
   int main() {
     int a = 1, b = 2, c = 3, d = 4;
     return (a+b)*(c+d) + (a+b)*(c+d);
   }
   ```
   观察汇编中 `addi sp, sp, -4/+4` 与 `sw/lw` 是否显著减少。

2. **循环热点变量**（验证局部变量 `lw/sw` 减少）
   ```c
   int main() {
     int sum = 0;
     int i = 0;
     while (i < 1000) { sum = sum + i; i = i + 1; }
     return sum;
   }
   ```
   理想情况下，`sum/i` 可长期驻留寄存器，循环体内 `lw/sw` 大幅减少。

---

## 2) 调用序列优化 + 栈 16B 对齐（减少参数搬运 + 修正 ABI）

### 2.1 RISC-V32 调用约定要点（最小集合）

- `a0-a7`：前 8 个整型参数寄存器；返回值在 `a0`
- 超过 8 个参数：放在 caller 的栈上（相对 `sp` 的连续槽位）
- `t0-t6/a0-a7`：caller-saved（被 call 破坏）
- `s0-s11`：callee-saved（被调用者若使用必须保存/恢复）
- **`sp` 在 call 边界必须 16 字节对齐**

### 2.2 现状瓶颈

当前实现为了保证“左到右求值 + 防止嵌套 call 破坏参数寄存器”，选择：

- 先把所有参数结果都存到一块临时栈区
- 再从临时区加载到 `a0-a7`
- 对第 9 个及之后的参数，还有一次额外的搬运

这会导致：

- 参数越多，内存搬运越多
- `sp` 调整次数偏多
- 对齐未系统保证（尤其当表达式求值过程中用 `-4/+4` 临时压栈时）

### 2.3 改进策略：一次分配，直接写入最终位置

在保持“参数左到右求值”的前提下，最简单的优化是：

- 仍然把前 8 个参数先 spill 到内存（避免被嵌套 call 破坏）
- 但**只 spill 这 8 个**（最多 32B），而不是 spill 全部参数
- 对第 9 个及之后的参数，**直接写到最终的出栈参数区**（避免二次搬运）

#### 2.3.1 栈布局建议（以 `sp` 为基准）

假设参数总数 `n`：

- `outStack = max(0, n-8) * 4`：真正的“出栈参数”区（传给 callee）
- `regSpill = min(8, n) * 4`：给 `a0-a7` 准备的 spill 区（仅用于保护左到右求值）
- `pad`：对齐用的填充，使得 call 前 `sp` 16B 对齐

分配一次：`sp -= (outStack + regSpill + pad)`

存放规则（左到右求值，逐个算到 `t0`）：

- 若 `i < 8`：写到 `sp + outStack + i*4`
- 若 `i >= 8`：写到 `sp + (i-8)*4`（已经是 callee 期望的位置）

求值结束后：

- 依次 `lw a0..a7, sp + outStack + i*4`
- `call foo`
- `sp += (outStack + regSpill + pad)`

这样可以做到：

- 参数只写一次（对 `i>=8` 的参数无二次搬运）
- 临时 spill 区恒定至多 32B
- `sp` 调整一次进入、一次退出

### 2.4 栈 16B 对齐：建议维护一个 `spDelta`（编译期）

问题根源：你的后端在表达式里会 `sp -= 4` 临时压栈，这会让 `sp` 在任意时刻可能是 `mod16 != 0`，从而 call 边界不对齐。

建议做一个“编译期的 sp 偏移追踪”：

- 在 `CodeGenerator` 里维护 `int spDeltaBytes`（相对函数 prologue 之后的 `sp`）
- 每次 emit `addi sp, sp, ±X`，同步更新 `spDeltaBytes += ±X`
- 生成 `CALL` 时，根据当前 `spDeltaBytes` 计算 `pad`，保证 `spDeltaBytes - (outStack+regSpill+pad)` 为 16 的倍数

这对 correctness 很关键：对齐不对会导致 ABI 不兼容（尤其是你后续引入更多调用/库函数时）。

### 2.5 验证与用例

- **嵌套调用**（验证 spill 保护正确）
  ```c
  int f(int a,int b,int c,int d,int e,int f,int g,int h,int i) { return i; }
  int g(int x) { return x+1; }
  int main() { return f(g(1),g(2),g(3),g(4),g(5),g(6),g(7),g(8),g(9)); }
  ```
  观察：`g()` 的 call 不应破坏已经求值的参数；第 9 个参数应落在栈出参区。

- **对齐检查（汇编级）**
  - 可以在 call 前插入调试输出：`andi t0, sp, 15` 并观察是否恒为 0（调试阶段用）。

---

## 3) 叶函数/栈帧优化（缩短 prologue/epilogue，减少无谓保存）

### 3.1 叶函数（Leaf Function）是什么

叶函数：函数体内不发生任何 `call`（包括表达式中的 `CallExpr`）。

对叶函数来说：

- `ra` 不会被覆盖（因为没有 `call` 写 `ra`）
- 因此通常不需要在 prologue 里 `sw ra,...`，也不需要在 epilogue 里 `lw ra,...`

### 3.2 分级实现（从保守到激进）

建议按收益/改动量分 3 档：

#### A 档：仅省掉 `ra` 的保存/恢复（低风险，高收益）

条件：函数内无 call。

做法：

- 仍然保留现有栈帧与 `s0`（帧指针）
- prologue 不再保存 `ra`
- epilogue 不再恢复 `ra`

优点：实现非常直接，不影响现有局部变量寻址逻辑。

#### B 档：按需保存 `s0`/按需建立栈帧（中等改动）

继续细分：

- 如果函数没有局部变量、没有需要 spill 的临时值、也不需要 `s0` 做基址：可以不建立帧指针
- 如果你的后端还会用 `sp -= 4` 作为临时栈，则“无栈帧”很难成立；所以通常要等方案 (1) 落地后再做

#### C 档：完全省略栈帧（激进，依赖 (1)）

条件更苛刻：

- 叶函数
- 无局部变量（或局部变量都寄存器化）
- 表达式求值不再用临时压栈（依赖寄存器分配）

这样 `main` 这类小函数可以变成几条指令直接 `ret`。

### 3.3 检测方式（建议用“每函数预分析”）

实现上建议在进入 `genFunc()` 前做一次扫描，收集：

- `hasCall`：是否包含 `CallExpr`
- `usesSRegs`：是否使用了（或计划分配）`s1-s11`（与方案 1.3 相关）
- `needsFrame`：是否需要栈空间（局部变量/出参区/spill/保存寄存器）

这些信息决定 prologue/epilogue 的形态与 frameSize。

### 3.4 验证与用例

1) 叶函数：
```c
int add(int a,int b){ return a+b; }
int main(){ return add(1,2); }
```
期望：`add` 的 prologue/epilogue 不保存/恢复 `ra`（如果你启用 A 档）。

2) 非叶函数：
```c
int id(int x){ return x; }
int f(int a){ return id(a)+1; }
```
期望：`f` 必须保存 `ra`（因为会 call）。

---

## 4) 汇编窥孔优化（Peephole, post-pass）

### 4.1 为什么需要窥孔优化

你当前后端是“结构化直接生成”，为了简单和正确，会产生一些局部冗余，例如：

- `mv t0, t0`（无效 move）
- 多次连续的 `addi sp, sp, ...`（可合并）
- `li`/`addi`/`mv` 等可互换简化的序列
- 比较/逻辑生成的固定模板里可能有可折叠的指令组合

窥孔优化的优势是：

- **不改动前端/中端结构**
- **只看小窗口指令序列，低风险**
- 能让代码立刻“更像手写汇编”

### 4.2 实现位置建议

在 `CodeGenerator::generate()` 生成完整 `assembly` 字符串后：

1. 按行拆分成指令列表（保留 label 行与注释行）
2. 对每个函数段（或全局）做 1~N 轮局部重写，直到不再变化或达到轮次上限
3. 再拼回字符串输出

### 4.3 推荐的“安全规则”（先做这些）

下面规则基本不依赖全局数据流分析，且安全性高：

1) 删除自拷贝：
- `mv rd, rs` 且 `rd == rs` → 删除

2) 合并相邻 sp 调整：
- `addi sp, sp, X` 紧跟 `addi sp, sp, Y` → 合并为 `addi sp, sp, X+Y`（若为 0 则删除）

3) store/load 对消（非常保守的版本）：
- `sw r, 0(sp)` 紧跟 `lw r, 0(sp)` 且中间无对 `sp` 的修改 → 删除 `lw`

4) 简化 `li 0/1` 后的比较模板（按你实际输出模板定制）：
- 例如 `sub t0, t0, t1; seqz t0, t0` 可保持（已经很短），但像 `li t0,0; ...; mv a0,t0` 等可再压缩

注意：不要一开始就做激进规则（例如跨多条指令的寄存器活跃性推断），否则容易误删。

### 4.4 验证方式

窥孔优化最适合用“文本匹配 + 行为回归”来验证：

- 对同一份 ToyC 输入，比较优化前后：
  - 指令条数
  - `lw/sw` 条数
  - `addi sp` 条数
- 再用 qemu/spike 跑一批测试用例，确保返回值一致（你已有测试建议用例，可复用）。

---

## 实施顺序建议（最稳妥的落地路线）

1. **先做 (2) 调用序列优化 + 16B 对齐**：影响面小，收益立刻可见，也能为后续引入更多函数/库调用打基础。
2. **再做 (3) 叶函数/栈帧优化**：同样是边界优化，容易验证，对小函数收益很高。
3. **再做 (4) 窥孔优化**：低风险补刀，能立刻“压掉模板冗余”。
4. **最后做 (1) 表达式/局部寄存器化**：收益最大但牵涉面广（寄存器活跃、call clobber、scope），建议最后攻坚。

---

## 度量指标（建议统一用这些看效果）

- **静态指标**：函数体指令条数、`lw/sw` 数量、`addi sp` 次数、`call` 周边指令数
- **动态指标**：在 qemu/spike 上跑 micro-benchmark 的周期/指令数（如果可用 perf 计数器更好）
- **回归正确性**：对一组 ToyC 程序，比较退出码（`main` 返回值）是否一致

