# 编译器优化问题分析报告

## 概述

本文档分析了 `compiler.cpp` 中优化器和代码生成器的潜在问题，这些问题可能导致优化效果不佳甚至产生反效果。

---

## 一、中端优化问题

### 1.1 公共子表达式消除 (CSE) 对单次表达式也提取 🔴 严重

**位置**: 第 1421-1431 行

**问题代码**:
```cpp
if (sig.length() > 5 && !hasCallExpr(newExpr.get())) {
    if (cseMap.count(sig)) {
        return make_unique<IdentExpr>(cseMap[sig]);  // ✓ 复用已有
    }
    // ⚠️ 问题：即使表达式只出现一次，也创建临时变量！
    string tmpName = "__cse_" + to_string(cseTempCount++);
    cseMap[sig] = tmpName;
    preStmts.push_back(make_unique<VarDeclStmt>(tmpName, move(newExpr)));
    return make_unique<IdentExpr>(tmpName);
}
```

**问题分析**:
- 当一个表达式**首次出现**时，代码直接创建临时变量并存储
- 如果这个表达式在后续代码中**从未再次出现**，这就是**反优化**
- 原本一条指令计算的表达式，变成了：计算 → 存储到变量 → 从变量读取

**示例**:
```c
// 源代码
int result = a * b + c;

// 错误的CSE结果（表达式只出现一次）
int __cse_0 = a * b;      // 多余的变量声明
int __cse_1 = __cse_0 + c; // 多余的变量声明
int result = __cse_1;      // 多余的间接引用
```

**正确做法**:
```cpp
// 第一遍：统计每个表达式的出现次数
// 第二遍：只对出现次数 >= 2 的表达式进行CSE
```

---

### 1.2 循环不变量外提太保守 🟡 中等

**位置**: 第 1361-1394 行

**问题代码**:
```cpp
for (auto it = body->stmts.begin(); it != body->stmts.end(); ) {
    Stmt* stmt = it->get();
    if (stmt->kind == StmtKind::VARDECL) {  // ⚠️ 只处理 VARDECL
        auto* varDecl = static_cast<VarDeclStmt*>(stmt);
        if (isLoopInvariant(varDecl->init.get(), modifiedVars)) {
            // ... 外提
        }
    }
    ++it;
}
```

**问题分析**:
1. **只外提 `VARDECL`**：赋值语句 `x = invariant_expr;` 不会被外提
2. **不处理嵌套结构**：循环体内 if/block 中的不变量被忽略
3. **不处理条件表达式**：`while (x < n * m)` 中的 `n * m` 不会被外提
4. **只检查顶层语句**：不会递归处理复合语句内部的不变量

**示例**:
```c
// 源代码
while (i < n * m) {       // n * m 是循环不变量，但不会被外提
    if (flag) {
        int t = a + b;    // 如果 a, b 不变，t 也不会被外提（嵌套在 if 内）
    }
    x = c * d;            // ASSIGN 语句，即使 c * d 不变也不会外提
    i = i + 1;
}
```

---

### 1.3 强度削减仅支持常量初始值 🟡 中等

**位置**: 第 1936-1950 行

**问题代码**:
```cpp
map<string, int> initVals;
for (size_t j = 0; j < i; j++) {
    if (stmts[j]->kind == StmtKind::VARDECL) {
        auto* v = static_cast<VarDeclStmt*>(stmts[j].get());
        // ⚠️ 只有常量初始化才记录
        if (inductionVars.count(v->name) && v->init->kind == ExprKind::NUMBER) {
            initVals[v->name] = static_cast<NumberExpr*>(v->init.get())->value;
        }
    } else if (stmts[j]->kind == StmtKind::ASSIGN) {
        auto* a = static_cast<AssignStmt*>(stmts[j].get());
        if (inductionVars.count(a->name) && a->value->kind == ExprKind::NUMBER) {
            initVals[a->name] = static_cast<NumberExpr*>(a->value.get())->value;
        }
    }
}
```

**问题分析**:
- 如果归纳变量初值不是字面常量，强度削减完全不会触发
- 例如 `int i = start;` 或 `int i = 0; ... i = n;` 都无法优化
  - 注：当前实现也会从循环前的 `ASSIGN` 收集字面常量初值，但若初值不是字面常量仍无法安全应用。

**示例**:
```c
// 这个循环无法进行强度削减，因为 i 的初值是变量
int i = start;
while (i < end) {
    use(i * 4);  // 无法转换为增量形式
    i = i + 1;
}
```

---

### 1.4 强度削减插入位置逻辑复杂且可能出错 🟡 中等

**位置**: 第 1966-1983 行

**问题代码**:
```cpp
for (auto& s : inLoopEnd) {
    bool inserted = false;
    for (size_t k = body->stmts.size(); k > 0; k--) {
        if (body->stmts[k-1]->kind == StmtKind::ASSIGN) {
            auto* assign = static_cast<AssignStmt*>(body->stmts[k-1].get());
            if (inductionVars.count(assign->name)) {
                body->stmts.insert(body->stmts.begin() + k - 1, move(s));
                inserted = true;
                break;
            }
        }
    }
    if (!inserted) {
        body->stmts.push_back(move(s));
    }
}
```

**问题分析**:
- 循环内多次 insert 会导致索引偏移
- 如果有多个归纳变量，插入顺序可能混乱
- 如果归纳变量更新在嵌套块内，找不到正确位置

---

## 二、后端优化问题

### 2.1 干涉图构建中 ASSIGN 未移除被定义变量 🔴 严重

**位置**: 第 3873-3885 行

**问题代码**:
```cpp
case StmtKind::ASSIGN: {
    auto* a = static_cast<AssignStmt*>(stmt);
    // 赋值目标与活跃变量干涉
    if (allVars.count(a->name)) {
        for (const string& liveVar : currentLive) {
            if (allVars.count(liveVar)) {
                interferenceGraph.addEdge(a->name, liveVar);
            }
        }
    }
    // ⚠️ 缺少: currentLive.erase(a->name);
    addUsedVarsToLive(a->value.get(), currentLive);
    break;
}
```

**对比 VARDECL 的正确实现** (第 3857-3871 行):
```cpp
case StmtKind::VARDECL: {
    auto* v = static_cast<VarDeclStmt*>(stmt);
    // ... 添加干涉边 ...
    currentLive.erase(v->name);  // ✓ 正确移除
    addUsedVarsToLive(v->init.get(), currentLive);
    break;
}
```

**问题分析**:
- 活跃变量分析中，定义点应该"杀死"变量的活跃性
- ASSIGN 缺少这一步，导致干涉图过于保守
- 结果：本可以复用同一寄存器的变量被分配到不同寄存器，或者被错误溢出

**示例**:
```c
int a = 1;
int b = 2;
a = 3;      // 在这之后，旧的 a 已死，b 和新 a 不应干涉
int c = a + b;
```

---

### 2.2 寄存器化变量赋值时不同步到栈 🟡 中等

**位置**: 第 4770-4772 行

**问题代码**:
```cpp
case StmtKind::ASSIGN: {
    auto* assign = static_cast<AssignStmt*>(stmt);
    genExpr(assign->value.get());
    if (g_optimize && varToReg.count(assign->name)) {
        emit("mv " + varToReg[assign->name] + ", t0");
        break;  // ⚠️ 直接返回，没有写回栈
    }
    // ... 栈上变量的处理 ...
}
```

**问题分析**:
- 如果变量被分配到寄存器，赋值只更新寄存器
- 但某些情况下（如取地址、跨函数调用）可能需要从栈读取
- 当前实现假设寄存器化变量永远从寄存器读取，但 `lookupVar` 返回的是栈偏移

**潜在问题场景**:
- 调试时查看变量值
- 某些边界情况下的变量访问路径

---

### 2.3 函数调用参数处理过于冗余 🟢 轻微

**位置**: 第 4656-4686 行

**当前实现**:
```cpp
// 1. 分配临时栈空间
emit("addi sp, sp, -" + to_string(totalAlloc));

// 2. 逐个求值参数并写入临时区
for (int i = 0; i < argCount; i++) {
    genExpr(call->args[i].get());
    emit("sw t0, " + to_string(tempOffset + i * 4) + "(sp)");
}

// 3. 从临时区加载到 a0-a7
for (int i = 0; i < argCount && i < 8; i++) {
    emit("lw a" + to_string(i) + ", " + to_string(tempOffset + i * 4) + "(sp)");
}
```

**问题分析**:
- 即使参数是简单的常量或变量，也要经过临时区中转
- 对于 `f(1, x)` 这样的调用，产生了不必要的 store/load

**优化建议**:
- 简单参数直接加载到目标寄存器
- 只有复杂表达式或需要保护的情况才使用临时区

---

### 2.4 窥孔优化遗漏常见模式 🟢 轻微

**位置**: 第 5048-5290 行

**遗漏的优化模式**:

1. **连续 store-load 同一位置**（代码里其实已实现 `sw r, X; lw r, X` 删除 `lw`）:
```asm
sw t0, -12(s0)
lw t0, -12(s0)    ; 可删除
```

2. **寄存器化变量的冗余 mv**:
```asm
mv s1, t0
mv t0, s1         ; 如果 t0 之后立即被覆盖，可删除
```

3. **条件跳转优化**（`beqz r, L1; j L2; L1:` 形式可转为 `bnez r, L2`）:
```asm
beqz t0, L1
j L2
L1:               ; 可以转换为 bnez t0, L2
```

4. **连续比较优化**:
```asm
slt t0, t1, t2
xori t0, t0, 1
xori t0, t0, 1    ; 已处理，但类似的 seqz/snez 组合可能遗漏
```

---

## 三、修复建议

### 3.1 高优先级修复

#### 修复 ASSIGN 干涉图问题
```cpp
case StmtKind::ASSIGN: {
    auto* a = static_cast<AssignStmt*>(stmt);
    if (allVars.count(a->name)) {
        for (const string& liveVar : currentLive) {
            if (allVars.count(liveVar)) {
                interferenceGraph.addEdge(a->name, liveVar);
            }
        }
    }
    currentLive.erase(a->name);  // 添加这一行
    addUsedVarsToLive(a->value.get(), currentLive);
    break;
}
```

#### 修复 CSE 单次表达式问题
```cpp
// 第一遍：统计表达式出现次数
map<string, int> exprCount;
void countExprOccurrences(Expr* expr) {
    if (expr->kind == ExprKind::BINARY) {
        string sig = exprToString(expr);
        exprCount[sig]++;
        // 递归处理子表达式
    }
}

// 第二遍：只对出现 >= 2 次的表达式进行 CSE
unique_ptr<Expr> cseExpr(Expr* expr, vector<unique_ptr<Stmt>>& preStmts) {
    // ...
    if (sig.length() > 5 && !hasCallExpr(newExpr.get())) {
        if (exprCount[sig] >= 2) {  // 添加出现次数检查
            if (cseMap.count(sig)) {
                return make_unique<IdentExpr>(cseMap[sig]);
            }
            string tmpName = "__cse_" + to_string(cseTempCount++);
            cseMap[sig] = tmpName;
            preStmts.push_back(make_unique<VarDeclStmt>(tmpName, move(newExpr)));
            return make_unique<IdentExpr>(tmpName);
        }
    }
    return newExpr;
}
```

### 3.2 中优先级修复

#### 增强循环不变量外提
- 处理 ASSIGN 语句
- 递归处理嵌套块
- 外提条件表达式中的不变子表达式

#### 增强强度削减
- 支持符号初始值（运行时计算初始值）
- 使用 SSA 形式简化分析

### 3.3 低优先级修复

#### 优化函数调用参数处理
- 识别简单参数直接加载
- 减少临时区使用

#### 扩展窥孔优化模式
- 添加更多 store-load 消除模式
- 添加条件跳转优化模式

---

## 四、测试建议

### 4.1 验证 CSE 修复
```c
int test_cse() {
    int a = 1, b = 2, c = 3;
    // 下面的 a+b 只出现一次，不应被提取
    int x = a + b;
    // 下面的 a*c 出现两次，应该被提取
    int y = a * c + 1;
    int z = a * c + 2;
    return x + y + z;
}
```

### 4.2 验证干涉图修复
```c
int test_regalloc() {
    int a = 1;
    int b = a + 1;
    a = 2;        // 旧 a 已死，新 a 可以和 b 复用寄存器
    int c = a + b;
    return c;
}
```

### 4.3 验证循环优化
```c
int test_loop(int n, int m) {
    int sum = 0;
    int limit = n * m;  // 应该在循环外计算
    int i = 0;
    while (i < limit) {
        sum = sum + i * 4;  // i * 4 应该用强度削减
        i = i + 1;
    }
    return sum;
}
```

---

## 五、总结

| 问题 | 严重程度 | 影响范围 | 修复难度 |
|------|----------|----------|----------|
| CSE 单次表达式提取 | 🔴 高 | 所有复杂表达式 | 中等 |
| ASSIGN 干涉图缺少 erase | 🔴 高 | 寄存器分配 | 简单 |
| 循环不变量外提保守 | 🟡 中 | 循环代码 | 中等 |
| 强度削减限制多 | 🟡 中 | 循环乘法 | 较难 |
| 函数调用参数冗余 | 🟢 低 | 函数调用 | 中等 |
| 窥孔优化遗漏 | 🟢 低 | 生成代码 | 简单 |

建议按照优先级从高到低逐步修复，每次修复后进行回归测试确保正确性。
