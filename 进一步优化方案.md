# ToyC 编译器进一步优化方案

## 当前已实现的优化

| 优化类别 | 具体优化 | 状态 |
|---------|---------|------|
| AST优化 | 常量折叠、常量传播、复制传播 | ✅ |
| AST优化 | 代数简化、强度削减 | ✅ |
| AST优化 | 死代码消除、死变量消除 | ✅ |
| AST优化 | 公共子表达式消除(CSE) | ✅ |
| AST优化 | 循环不变量外提 | ✅ |
| AST优化 | 循环展开(完全+部分) | ✅ |
| AST优化 | 函数内联 | ✅ |
| 代码生成 | 叶函数优化(不保存ra) | ✅ |
| 代码生成 | 局部变量寄存器化(s1-s6) | ✅ |
| 代码生成 | 乘除法强度削减(移位替代) | ✅ |
| 后处理 | 窥孔优化(19种模式) | ✅ |

---

## 方案一：尾调用优化 (TCO)

### 原理
将尾递归调用转换为循环，避免栈空间增长。

### 适用场景
```c
// 尾递归形式
int factorial(int n, int acc) {
    if (n <= 1) return acc;
    return factorial(n - 1, n * acc);  // 尾调用
}
```

### 实现要点
1. **检测尾调用**：`return func(...)` 形式，且调用后无其他操作
2. **转换为循环**：
   ```c
   // 转换后
   int factorial(int n, int acc) {
   loop:
       if (n <= 1) return acc;
       int tmp_n = n - 1;
       int tmp_acc = n * acc;
       n = tmp_n;
       acc = tmp_acc;
       goto loop;
   }
   ```
3. **汇编层面**：直接跳转到函数入口，复用栈帧

### 生成代码对比
```asm
# 优化前（每次调用分配栈帧）
factorial:
    addi sp, sp, -16
    sw ra, 12(sp)
    ...
    call factorial
    ...
    ret

# 优化后（循环复用栈帧）
factorial:
    addi sp, sp, -16
    sw ra, 12(sp)
.L_loop:
    ...
    # 更新参数
    mv a0, t0
    mv a1, t1
    j .L_loop
    ...
    ret
```

### 当前状态
已实现但禁用（第2775-2778行），需要修复空指针问题后重新启用。

---

## 方案二：条件移动优化

### 原理
用算术指令替代短分支，避免分支预测失败的惩罚。

### 适用场景
```c
int abs(int x) {
    if (x < 0) return -x;
    return x;
}

int max(int a, int b) {
    if (a > b) return a;
    return b;
}
```

### 实现方法
```asm
# 优化前（有分支）
abs:
    bgez a0, .L1
    neg a0, a0
.L1:
    ret

# 优化后（无分支）
abs:
    srai t0, a0, 31      # t0 = (x < 0) ? -1 : 0
    xor a0, a0, t0       # 条件取反
    sub a0, a0, t0       # 条件加1
    ret

# max 函数优化
max:
    slt t0, a0, a1       # t0 = (a < b) ? 1 : 0
    neg t0, t0           # t0 = (a < b) ? -1 : 0
    xor t1, a0, a1       # t1 = a ^ b
    and t1, t1, t0       # t1 = (a < b) ? (a ^ b) : 0
    xor a0, a0, t1       # a0 = (a < b) ? b : a
    ret
```

### 实现要点
1. 识别简单的 if-else 模式
2. 检查两个分支都是简单赋值
3. 使用 `slt` + `neg` + `xor` 组合实现条件选择

---

## 方案三：指令调度

### 原理
重排指令顺序，隐藏流水线延迟（特别是 load-use 延迟）。

### RISC-V 流水线延迟
| 指令类型 | 延迟周期 |
|---------|---------|
| ALU指令 | 1 |
| 分支指令 | 2-3 |
| Load指令 | 2 |
| Mul指令 | 3-5 |
| Div指令 | 10-40 |

### 优化示例
```asm
# 优化前（load-use 停顿）
lw t0, 0(s0)
add t1, t0, t2      # 停顿1周期等待t0
lw t3, 4(s0)
add t4, t3, t5      # 停顿1周期等待t3

# 优化后（交错调度）
lw t0, 0(s0)
lw t3, 4(s0)        # 填充t0的延迟槽
add t1, t0, t2      # t0已就绪
add t4, t3, t5      # t3已就绪
```

### 实现方法
1. 构建数据依赖图(DDG)
2. 使用列表调度算法
3. 考虑寄存器压力约束

---

## 方案四：循环优化增强

### 4.1 循环反转 (Loop Inversion)

```c
// 优化前
while (i < n) {
    body;
    i++;
}

// 优化后（减少一次条件判断）
if (i < n) {
    do {
        body;
        i++;
    } while (i < n);
}
```

生成代码：
```asm
# 优化前
.L_cond:
    bge a0, a1, .L_end
    # body
    j .L_cond
.L_end:

# 优化后
    bge a0, a1, .L_end
.L_body:
    # body
    blt a0, a1, .L_body
.L_end:
```

### 4.2 循环分裂 (Loop Fission)

```c
// 优化前（缓存不友好）
for (int i = 0; i < n; i++) {
    a[i] = b[i] + 1;
    c[i] = d[i] * 2;
}

// 优化后（更好的缓存局部性）
for (int i = 0; i < n; i++) {
    a[i] = b[i] + 1;
}
for (int i = 0; i < n; i++) {
    c[i] = d[i] * 2;
}
```

### 4.3 循环融合 (Loop Fusion)

```c
// 优化前（两次遍历）
for (int i = 0; i < n; i++) sum1 += a[i];
for (int i = 0; i < n; i++) sum2 += b[i];

// 优化后（一次遍历）
for (int i = 0; i < n; i++) {
    sum1 += a[i];
    sum2 += b[i];
}
```

---

## 方案五：寄存器分配改进

### 5.1 图着色寄存器分配

当前使用简单的频率排序分配，可改进为：

1. **构建干涉图**：变量同时活跃则有边
2. **图着色**：K着色问题（K=可用寄存器数）
3. **溢出处理**：无法着色的变量溢出到栈

### 5.2 活跃变量分析

```
// 数据流方程
IN[B] = USE[B] ∪ (OUT[B] - DEF[B])
OUT[B] = ∪ IN[S] for all successors S of B
```

### 5.3 溢出代码优化

```asm
# 优化前（每次使用都加载）
lw t0, -16(s0)
add t1, t0, t2
sw t1, -20(s0)
lw t0, -16(s0)    # 冗余加载
add t3, t0, t4

# 优化后（寄存器重用）
lw t0, -16(s0)
add t1, t0, t2
sw t1, -20(s0)
add t3, t0, t4    # 复用t0
```

---

## 方案六：参数传递优化

### 当前问题
所有参数都保存到栈，即使只读取一次。

### 优化方案
1. **延迟保存**：只在参数被修改或需要地址时才保存
2. **直接使用**：只读参数直接使用 a0-a7

```asm
# 优化前
foo:
    addi sp, sp, -32
    sw a0, 0(sp)      # 保存参数
    sw a1, 4(sp)
    lw t0, 0(sp)      # 再读取
    lw t1, 4(sp)
    add a0, t0, t1
    ret

# 优化后
foo:
    add a0, a0, a1    # 直接使用a0, a1
    ret
```

---

## 方案七：分支布局优化

### 原理
将热路径放在 fall-through 位置，减少跳转。

### 实现
```asm
# 优化前（unlikely 在 fall-through）
    beqz t0, .L_unlikely
    # likely code (需要跳转才能到达)
    j .L_end
.L_unlikely:
    # unlikely code
.L_end:

# 优化后（likely 在 fall-through）
    bnez t0, .L_likely
    # unlikely code
    j .L_end
.L_likely:
    # likely code (fall-through)
.L_end:
```

### 启发式规则
- 循环体内是 likely
- 错误处理是 unlikely
- return 语句是 unlikely（相对于继续执行）

---

## 方案八：加载存储优化

### 8.1 冗余加载消除

```asm
# 优化前
sw t0, 0(s0)
# ... 无修改 0(s0) ...
lw t1, 0(s0)      # 冗余，可用 mv t1, t0

# 优化后
sw t0, 0(s0)
mv t1, t0
```

### 8.2 存储下沉

```asm
# 优化前
sw t0, 0(s0)      # 可能被覆盖
# ... 计算 ...
sw t1, 0(s0)      # 覆盖

# 优化后
# ... 计算 ...
sw t1, 0(s0)      # 只保留最后一次
```

### 8.3 加载提升

```asm
# 优化前（循环内重复加载）
.L_loop:
    lw t0, 0(s0)  # 循环不变量
    add t1, t1, t0
    j .L_loop

# 优化后
lw t0, 0(s0)      # 提升到循环外
.L_loop:
    add t1, t1, t0
    j .L_loop
```

---

## 实现优先级建议

| 优先级 | 优化方案 | 收益 | 复杂度 |
|-------|---------|------|-------|
| 1 | 尾调用优化 | 高（递归场景） | 中 |
| 2 | 条件移动优化 | 中 | 低 |
| 3 | 参数传递优化 | 中 | 低 |
| 4 | 循环反转 | 中 | 低 |
| 5 | 指令调度 | 中 | 高 |
| 6 | 图着色寄存器分配 | 高 | 高 |
| 7 | 加载存储优化 | 中 | 中 |
| 8 | 分支布局优化 | 低 | 中 |

---

## 测试用例建议

```c
// test_tco.c - 尾调用优化测试
int factorial_tail(int n, int acc) {
    if (n <= 1) return acc;
    return factorial_tail(n - 1, n * acc);
}

// test_cmov.c - 条件移动优化测试
int abs_val(int x) {
    if (x < 0) return -x;
    return x;
}

int max_val(int a, int b) {
    if (a > b) return a;
    return b;
}

// test_loop_inv.c - 循环反转测试
int sum_to_n(int n) {
    int sum = 0;
    int i = 1;
    while (i <= n) {
        sum = sum + i;
        i = i + 1;
    }
    return sum;
}
```
