# ToyC 编译器 - 生成代码运行时优化方案（最高强度）

## 优化目标

**目标**：最大化生成代码的执行效率，减少运行时间

**注意**：本文档聚焦于**生成代码的运行时优化**，而非编译器本身的编译时间优化。

---

## 一、优化分类说明

### 1.1 编译时间优化 vs 运行时优化

| 类型 | 目标 | 示例 |
|------|------|------|
| **编译时间优化** | 让编译器跑得更快 | 静态关键字表、避免 dynamic_cast |
| **运行时优化** | 让生成的代码跑得更快 | 常量折叠、循环展开、寄存器分配 |

### 1.2 当前编译器中的编译时间优化（非本文档重点）

| 位置 | 优化 | 说明 |
|------|------|------|
| Lexer 第81行 | 静态 unordered_map | 关键字表只初始化一次，O(1) 查找 |
| AST 第154-156行 | ExprKind/StmtKind 枚举 | 替代 dynamic_cast，避免 RTTI 开销 |
| CodeGenerator | switch + static_cast | 使用枚举判断节点类型 |

**这些优化让编译器运行更快，但不影响生成代码的执行效率。**

---

## 二、已实现的运行时优化（Optimizer 类）

### 2.1 中端优化（AST 层）

| 优化技术 | 位置 | 效果 |
|----------|------|------|
| 常量折叠 | foldExpr() | `3+4` → `7`，编译期计算 |
| 常量传播 | foldExpr() | `x=5; return x` → `return 5` |
| 复制传播 | foldExpr() | `y=x; return y` → `return x` |
| 代数简化 | foldExpr() | `x*1=x`, `x+0=x`, `x-x=0` |
| 短路求值优化 | foldExpr() | `0 && x` → `0`, `1 \|\| x` → `1` |
| 死代码消除 | optimizeStmtList() | 删除 `if(0)`, `while(0)`, return 后代码 |
| 空语句消除 | optimizeStmtList() | 删除 `;` 和空块 `{}` |
| 自赋值消除 | optimizeStmtList() | 删除 `x = x` |
| 无副作用表达式删除 | optimizeStmtList() | 删除 `x + y;`（无函数调用） |

### 2.2 循环优化

| 优化技术 | 位置 | 效果 |
|----------|------|------|
| 循环不变量外提 | hoistLoopInvariants() | 将不变计算移出循环 |
| 归纳变量强度削减 | optimizeLoopStrengthReduction() | `i * 4` → 增量 `+4` |

### 2.3 高级优化

| 优化技术 | 位置 | 效果 |
|----------|------|------|
| 公共子表达式消除 (CSE) | cseStmtList() | 避免重复计算相同表达式 |
| 尾递归优化 | optimizeTailRecursion() | 尾递归转换为 while 循环 |
| 死变量消除 | eliminateDeadVars() | 删除未使用的变量声明 |

---

## 三、待实现的运行时优化（最高强度）

### 3.1 代码生成层优化（高优先级）

#### 3.1.1 乘法强度削减 - 2的幂优化

**当前问题**（第1924行）：
```cpp
if (binary->op == "*") emit("mul t0, t0, t1");  // 直接生成 mul，无优化
```

**优化方案**：
```asm
# 优化前
mul t0, t0, 8    # 乘法需要多周期

# 优化后
slli t0, t0, 3   # 左移3位，单周期
```

**覆盖场景**：
| 表达式 | 优化后 | 收益 |
|--------|--------|------|
| `x * 2` | `slli x, 1` | ~5x |
| `x * 4` | `slli x, 2` | ~5x |
| `x * 8` | `slli x, 3` | ~5x |
| `x * 3` | `slli t1, x, 1; add x, t1, x` | ~2x |
| `x * 5` | `slli t1, x, 2; add x, t1, x` | ~2x |
| `x * 7` | `slli t1, x, 3; sub x, t1, x` | ~2x |
| `x * 9` | `slli t1, x, 3; add x, t1, x` | ~2x |

**实现代码**：
```cpp
// 在 genExpr() 的 BINARY 分支，处理乘法前添加
if (binary->op == "*") {
    // 检查右操作数是否为常量
    if (binary->right->kind == ExprKind::NUMBER) {
        int val = static_cast<NumberExpr*>(binary->right.get())->value;

        // 2的幂 -> 移位
        if (val > 0 && (val & (val - 1)) == 0) {
            genExpr(binary->left.get());
            int shift = 0;
            while ((1 << shift) < val) shift++;
            emit("slli t0, t0, " + to_string(shift));
            break;
        }

        // x * 3 = (x << 1) + x
        if (val == 3) {
            genExpr(binary->left.get());
            emit("slli t1, t0, 1");
            emit("add t0, t1, t0");
            break;
        }

        // x * 5 = (x << 2) + x
        if (val == 5) {
            genExpr(binary->left.get());
            emit("slli t1, t0, 2");
            emit("add t0, t1, t0");
            break;
        }

        // x * 7 = (x << 3) - x
        if (val == 7) {
            genExpr(binary->left.get());
            emit("slli t1, t0, 3");
            emit("sub t0, t1, t0");
            break;
        }

        // x * 9 = (x << 3) + x
        if (val == 9) {
            genExpr(binary->left.get());
            emit("slli t1, t0, 3");
            emit("add t0, t1, t0");
            break;
        }
    }

    // 检查左操作数是否为常量（交换律）
    if (binary->left->kind == ExprKind::NUMBER) {
        // 同上处理
    }
}
```

#### 3.1.2 除法强度削减 - 2的幂优化

**当前问题**（第1925行）：
```cpp
if (binary->op == "/") emit("div t0, t0, t1");  // 直接生成 div
```

除法是最慢的运算（可能 30+ 周期），必须优化。

**优化方案**（有符号除法需要处理负数舍入）：
```asm
# x / 4 (有符号)
# 优化前
div t0, t0, 4    # 30+ 周期

# 优化后
srai t1, t0, 31      # 符号位扩展到 t1
srli t1, t1, 30      # 取低2位作为调整值
add t0, t0, t1       # 负数加偏移
srai t0, t0, 2       # 算术右移
```

**实现代码**：
```cpp
if (binary->op == "/") {
    if (binary->right->kind == ExprKind::NUMBER) {
        int val = static_cast<NumberExpr*>(binary->right.get())->value;

        if (val > 0 && (val & (val - 1)) == 0) {
            genExpr(binary->left.get());
            int shift = 0;
            while ((1 << shift) < val) shift++;

            if (shift > 0) {
                // 有符号除法需要处理负数舍入
                emit("srai t1, t0, 31");                    // 符号位扩展
                emit("srli t1, t1, " + to_string(32 - shift)); // 调整值
                emit("add t0, t0, t1");                     // 加偏移
            }
            emit("srai t0, t0, " + to_string(shift));
            break;
        }
    }
}
```

#### 3.1.3 取模强度削减 - 2的幂优化

**当前问题**（第1926行）：
```cpp
if (binary->op == "%") emit("rem t0, t0, t1");  // 直接生成 rem
```

**优化方案**（无符号或已知非负时）：
```asm
# x % 8 (x >= 0)
# 优化前
rem t0, t0, 8

# 优化后
andi t0, t0, 7   # 单周期
```

**实现代码**：
```cpp
if (binary->op == "%") {
    if (binary->right->kind == ExprKind::NUMBER) {
        int val = static_cast<NumberExpr*>(binary->right.get())->value;

        if (val > 0 && (val & (val - 1)) == 0) {
            genExpr(binary->left.get());
            // 简化版本（假设非负）
            emit("andi t0, t0, " + to_string(val - 1));
            break;
        }
    }
}
```

---

### 3.2 循环优化（高优先级）

#### 3.2.1 循环完全展开 (Loop Unrolling)

**原理**：减少循环控制开销（条件判断、跳转）

```c
// 优化前
int i = 0;
while (i < 4) {
    sum = sum + i;
    i = i + 1;
}

// 优化后 - 完全展开
sum = sum + 0;
sum = sum + 1;
sum = sum + 2;
sum = sum + 3;
```

**展开条件**：
1. 循环次数编译期已知
2. 循环次数 ≤ 16
3. 循环体 ≤ 5 条语句

**实现代码**：
```cpp
void unrollKnownLoops(vector<unique_ptr<Stmt>>& stmts) {
    for (size_t i = 0; i < stmts.size(); i++) {
        if (stmts[i]->kind != StmtKind::WHILE) continue;
        auto* whileStmt = static_cast<WhileStmt*>(stmts[i].get());

        // 检测模式: int i = init; while (i < bound) { ...; i = i + step; }
        if (i == 0) continue;
        if (stmts[i-1]->kind != StmtKind::VARDECL) continue;

        auto* initStmt = static_cast<VarDeclStmt*>(stmts[i-1].get());
        if (initStmt->init->kind != ExprKind::NUMBER) continue;

        string inductionVar = initStmt->name;
        int initVal = static_cast<NumberExpr*>(initStmt->init.get())->value;

        // 检查条件 i < const
        if (whileStmt->cond->kind != ExprKind::BINARY) continue;
        auto* cond = static_cast<BinaryExpr*>(whileStmt->cond.get());
        if (cond->op != "<") continue;
        if (cond->left->kind != ExprKind::IDENT) continue;
        if (cond->right->kind != ExprKind::NUMBER) continue;
        if (static_cast<IdentExpr*>(cond->left.get())->name != inductionVar) continue;

        int bound = static_cast<NumberExpr*>(cond->right.get())->value;
        int iterations = bound - initVal;

        // 只展开小循环
        if (iterations <= 0 || iterations > 16) continue;

        // 检测步长并展开...
    }
}
```

#### 3.2.2 循环部分展开

**适用场景**：循环次数未知或过大

```c
// 优化前
while (i < n) {
    sum = sum + arr[i];
    i = i + 1;
}

// 优化后（4倍展开）
while (i + 4 <= n) {
    sum = sum + arr[i];
    sum = sum + arr[i+1];
    sum = sum + arr[i+2];
    sum = sum + arr[i+3];
    i = i + 4;
}
// 余数处理
while (i < n) {
    sum = sum + arr[i];
    i = i + 1;
}
```

---

### 3.3 函数优化（高优先级）

#### 3.3.1 函数内联 (Function Inlining)

**原理**：消除函数调用开销（压栈、跳转、返回）

```c
// 优化前
int square(int x) { return x * x; }
int main() {
    int a = square(5);
    return a;
}

// 优化后
int main() {
    int __inline_x = 5;
    int a = __inline_x * __inline_x;
    return a;
}
```

**内联条件**：
1. 函数体 ≤ 10 条语句
2. 非递归函数
3. 调用次数 ≤ 3 次（避免代码膨胀）
4. 参数个数 ≤ 4

#### 3.3.2 尾调用优化 (Tail Call Optimization)

**已实现尾递归优化**，可扩展到一般尾调用：

```c
// 优化前
int foo(int x) {
    if (x == 0) return 0;
    return bar(x - 1);  // 尾调用 bar
}

// 优化后 - 复用栈帧，跳转替代调用
```

---

### 3.4 后端优化

#### 3.4.1 寄存器分配优化

**当前问题**（第1914-1920行）：
```cpp
// 每次二元运算都压栈
genExpr(binary->left.get());
emit("addi sp, sp, -4");
emit("sw t0, 0(sp)");      // 压栈保存
genExpr(binary->right.get());
emit("mv t1, t0");
emit("lw t0, 0(sp)");      // 弹栈恢复
emit("addi sp, sp, 4");
```

只使用 t0/t1，浪费了 t2-t6 五个寄存器。

**优化方案**：使用寄存器栈

```cpp
class RegisterAllocator {
    const char* regs[7] = {"t0", "t1", "t2", "t3", "t4", "t5", "t6"};
    int top = 0;

    const char* alloc() {
        if (top < 7) return regs[top++];
        // 溢出处理...
        return nullptr;
    }

    void free() { if (top > 0) top--; }
};
```

**优化效果**：
```asm
# 优化前 (a + b) * (c + d)
lw t0, a
lw t1, b
add t0, t0, t1
addi sp, sp, -4
sw t0, 0(sp)     # 压栈
lw t0, c
lw t1, d
add t0, t0, t1
mv t1, t0
lw t0, 0(sp)
addi sp, sp, 4   # 弹栈
mul t0, t0, t1

# 优化后
lw t0, a
lw t1, b
add t2, t0, t1   # 使用 t2
lw t3, c
lw t4, d
add t5, t3, t4   # 使用 t5
mul t0, t2, t5   # 无需压栈
```

#### 3.4.2 窥孔优化 (Peephole Optimization)

**原理**：识别并优化局部指令序列

| 模式 | 优化 | 说明 |
|------|------|------|
| `mv t0, t0` | 删除 | 无效移动 |
| `li t0, 0; add t0, t0, t1` | `mv t0, t1` | 加0简化 |
| `addi t0, t0, 0` | 删除 | 无效加法 |
| `sw t0, X(sp); lw t0, X(sp)` | 删除 lw | 冗余加载 |
| `j L; L:` | 删除 j | 无效跳转 |
| `li t0, 1; li t0, 2` | `li t0, 2` | 死存储 |

**实现**：生成汇编后进行后处理扫描

```cpp
class PeepholeOptimizer {
    vector<string> lines;

    void optimize() {
        bool changed = true;
        while (changed) {
            changed = false;
            for (size_t i = 0; i < lines.size(); i++) {
                // 模式匹配和替换...
            }
        }
    }
};
```

---

## 四、实现优先级

### 第一阶段（高收益，低-中难度）

| 序号 | 优化 | 收益 | 难度 | 位置 |
|------|------|------|------|------|
| 1 | 乘法 2^n → 移位 | 高 | 低 | CodeGenerator |
| 2 | 除法 2^n → 移位 | 高 | 中 | CodeGenerator |
| 3 | 取模 2^n → 与运算 | 高 | 低 | CodeGenerator |
| 4 | 乘法分解 (x*3,5,7,9) | 中 | 低 | CodeGenerator |

### 第二阶段（中等收益，中等难度）

| 序号 | 优化 | 收益 | 难度 | 位置 |
|------|------|------|------|------|
| 5 | 循环完全展开 | 高 | 中 | Optimizer |
| 6 | 函数内联 | 高 | 中 | Optimizer |
| 7 | 窥孔优化 | 中 | 低 | CodeGenerator 后处理 |

### 第三阶段（深度优化，较高难度）

| 序号 | 优化 | 收益 | 难度 | 位置 |
|------|------|------|------|------|
| 8 | 多寄存器分配 | 中 | 高 | CodeGenerator |
| 9 | 循环部分展开 | 中 | 中 | Optimizer |
| 10 | 尾调用优化（非递归） | 低 | 中 | Optimizer/CodeGenerator |

---

## 五、测试用例

### 5.1 乘法优化测试

```c
int main() {
    int x = 10;
    int a = x * 2;   // 应生成 slli t0, t0, 1
    int b = x * 8;   // 应生成 slli t0, t0, 3
    int c = x * 3;   // 应生成 slli + add
    return a + b + c;
}
```

### 5.2 除法优化测试

```c
int main() {
    int x = 100;
    int a = x / 2;   // 应生成 srai
    int b = x / 4;   // 应生成 srai
    return a + b;
}
```

### 5.3 循环展开测试

```c
int main() {
    int sum = 0;
    int i = 0;
    while (i < 4) {
        sum = sum + i;
        i = i + 1;
    }
    return sum;  // 应完全展开并常量折叠为 return 6
}
```

---

## 六、预期优化效果

| 场景 | 未优化运行时间 | 优化后运行时间 | 提升 |
|------|----------------|----------------|------|
| 2的幂乘法 | 100% | 15-20% | 80-85% |
| 2的幂除法 | 100% | 10-15% | 85-90% |
| 小函数调用 | 100% | 40-50% | 50-60% |
| 固定次数循环 | 100% | 30-50% | 50-70% |
| 复杂表达式 | 100% | 60-70% | 30-40% |
| **综合** | 100% | 40-60% | 40-60% |

---

## 七、实施步骤

### 步骤1：乘法/除法/取模 2的幂优化

修改 `CodeGenerator::genExpr()` 的 `BINARY` 分支：

```cpp
case ExprKind::BINARY: {
    auto* binary = static_cast<BinaryExpr*>(expr);

    // ========== 乘法优化 ==========
    if (binary->op == "*") {
        // 右操作数是常量
        if (binary->right->kind == ExprKind::NUMBER) {
            int val = static_cast<NumberExpr*>(binary->right.get())->value;

            // 2的幂
            if (val > 0 && (val & (val - 1)) == 0) {
                genExpr(binary->left.get());
                int shift = 0;
                int tmp = val;
                while (tmp > 1) { tmp >>= 1; shift++; }
                emit("slli t0, t0, " + to_string(shift));
                break;
            }
            // x * 3
            if (val == 3) {
                genExpr(binary->left.get());
                emit("slli t1, t0, 1");
                emit("add t0, t1, t0");
                break;
            }
            // x * 5
            if (val == 5) {
                genExpr(binary->left.get());
                emit("slli t1, t0, 2");
                emit("add t0, t1, t0");
                break;
            }
            // x * 7
            if (val == 7) {
                genExpr(binary->left.get());
                emit("slli t1, t0, 3");
                emit("sub t0, t1, t0");
                break;
            }
        }
        // 左操作数是常量（交换律）- 类似处理
    }

    // ========== 除法优化 ==========
    if (binary->op == "/" && binary->right->kind == ExprKind::NUMBER) {
        int val = static_cast<NumberExpr*>(binary->right.get())->value;
        if (val > 0 && (val & (val - 1)) == 0) {
            genExpr(binary->left.get());
            int shift = 0;
            int tmp = val;
            while (tmp > 1) { tmp >>= 1; shift++; }

            // 有符号除法舍入修正
            if (shift > 0) {
                emit("srai t1, t0, 31");
                emit("srli t1, t1, " + to_string(32 - shift));
                emit("add t0, t0, t1");
            }
            emit("srai t0, t0, " + to_string(shift));
            break;
        }
    }

    // ========== 取模优化 ==========
    if (binary->op == "%" && binary->right->kind == ExprKind::NUMBER) {
        int val = static_cast<NumberExpr*>(binary->right.get())->value;
        if (val > 0 && (val & (val - 1)) == 0) {
            genExpr(binary->left.get());
            emit("andi t0, t0, " + to_string(val - 1));
            break;
        }
    }

    // ... 原有的普通二元运算处理 ...
}
```

### 步骤2：在 Optimizer 中添加循环展开

在 `Optimizer::optimize()` 中添加新阶段。

### 步骤3：添加函数内联支持

创建 `Inliner` 类，在语义分析后执行。

### 步骤4：添加窥孔优化

在 `CodeGenerator::generate()` 返回前进行后处理。
