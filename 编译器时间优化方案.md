# ToyC 编译器时间性能优化方案

## 已实施的优化（2026-01-02）

### 已完成优化项 - 基础优化

| 优化项 | 状态 | 说明 |
|--------|------|------|
| `-opt` 命令行参数 | ✅ 完成 | 通过 `-opt` 启用优化 |
| 静态 unordered_map | ✅ 完成 | 关键字表只初始化一次，O(1) 查找 |
| 枚举替代 dynamic_cast | ✅ 完成 | 使用 switch + static_cast，避免 RTTI 开销 |
| 常量折叠优化 | ✅ 完成 | 编译期计算常量表达式 |
| 代数简化 | ✅ 完成 | `x+0=x`, `x*1=x`, `x*0=0`, `x-x=0`, `x/x=1` 等 |

### 已完成优化项 - 深度优化（针对性能测试）

| 优化项 | 对应测试 | 说明 |
|--------|----------|------|
| 常量传播 | p01_const | 变量赋值常量后，后续使用直接替换 |
| 复制传播 | p03_copy | `x=y` 后，使用 x 处替换为 y |
| 死代码消除 | p02_dead_code | 删除 `if(0)`, `while(0)`, return 后代码 |
| 自赋值消除 | p03_copy | 删除 `x = x` |
| 无副作用表达式删除 | p02_dead_code | 删除不含函数调用的表达式语句 |
| 短路求值优化 | p05_algebra | `0 && x = 0`, `1 \|\| x = 1` |
| 尾递归优化 | p06_tail_recursion | 尾递归转换为循环 |
| 多轮迭代优化 | 综合 | 最多10轮优化直到无变化 |

### 已完成优化项 - 高级优化（循环优化）

| 优化项 | 对应测试 | 说明 |
|--------|----------|------|
| 循环不变量外提 | p07_loop | 将循环中不变的计算移到循环外 |
| 公共子表达式消除 (CSE) | p04_common_subexpr | 避免重复计算相同表达式 |
| 死变量消除 | p02_dead_code | 删除未使用的变量声明 |
| 归纳变量强度削减 | p07_loop | `i * c` 转换为增量加法 `sum += step` |

### 优化效果示例

**常量传播 + 复制传播**:
```c
int main() { int x = 5; int y = x; return y; }
// 优化后：return 5（x 和 y 都被替换为常量 5）
```

**死代码消除**:
```c
int main() { if (0) { return 1; } return 2; }
// 优化后：直接 return 2（if(0) 分支被删除）
```

**归纳变量强度削减**:
```c
int main() {
    int sum = 0; int i = 0;
    while (i < 10) { sum = sum + i * 4; i = i + 1; }
    return sum;
}
// 优化后：i * 4 被替换为辅助变量 __sr_0
// 每次循环只执行 __sr_0 = __sr_0 + 4，避免乘法
```

### 优化阶段

编译器优化分为7个阶段：

1. **基础优化**（10轮）：常量传播、复制传播、常量折叠、代数简化、死代码消除
2. **循环不变量外提**：将循环中不变的表达式移到循环外
3. **归纳变量强度削减**：将循环中的乘法转换为增量加法
4. **公共子表达式消除**：避免重复计算
5. **尾递归优化**：将尾递归转换为循环
6. **死变量消除**（3轮）：删除未使用的变量
7. **最终基础优化**（5轮）：再次运行基础优化清理

---

## 一、当前性能瓶颈分析

### 1.1 词法分析器瓶颈

| 问题 | 位置 | 影响 |
|------|------|------|
| 关键字表每次重建 | `tokenize()` 第75行 | 每次调用都创建 `map<string, TokenType>` |
| 使用 `std::map` | 关键字查找 | O(log n) 查找，不如哈希表 |
| 字符串逐字符拼接 | 标识符/数字解析 | `id += peek()` 导致多次内存分配 |

### 1.2 语法分析器瓶颈

| 问题 | 位置 | 影响 |
|------|------|------|
| 大量 `make_unique` 调用 | 所有 parse 方法 | 频繁堆内存分配 |
| Token 按值传递 | `consume()` 返回值 | 不必要的拷贝 |

### 1.3 代码生成器瓶颈（最严重）

| 问题 | 位置 | 影响 |
|------|------|------|
| 大量 `dynamic_cast` | `genExpr()`/`genStmt()` | RTTI 开销大，每个节点都要判断类型 |
| 二元运算压栈 | `genExpr()` 536-542行 | 每次运算都 push/pop 栈，生成冗余指令 |
| 只用 t0/t1 寄存器 | 表达式求值 | 未利用其他临时寄存器，内存访问多 |
| 字符串拼接生成汇编 | `emit()` | `to_string()` + 拼接开销 |

---

## 二、优化方案

### 2.1 词法分析器优化

#### 优化1：静态关键字表
```cpp
// 改进前：每次 tokenize() 都创建
map<string, TokenType> keywords = {...};

// 改进后：使用静态 unordered_map
static const unordered_map<string, TokenType> keywords = {
    {"int", TokenType::INT}, {"void", TokenType::VOID},
    {"if", TokenType::IF}, {"else", TokenType::ELSE},
    {"while", TokenType::WHILE}, {"break", TokenType::BREAK},
    {"continue", TokenType::CONTINUE}, {"return", TokenType::RETURN}
};
```

**收益**:
- 关键字表只初始化一次
- O(1) 平均查找时间

#### 优化2：预分配字符串空间
```cpp
// 改进前
string id;
while (...) { id += peek(); advance(); }

// 改进后
string id;
id.reserve(32);  // 预分配空间
size_t start = pos;
while (pos < source.length() && (isalnum(source[pos]) || source[pos] == '_')) pos++;
id = source.substr(start, pos - start);
if (source[start] != '\n') {} else line++;  // 需要单独处理换行
```

**收益**: 减少内存重分配次数

#### 优化3：批量读取优化
```cpp
// 改进后：使用 string_view (C++17) 避免拷贝
string_view sourceView;
```

---

### 2.2 语法分析器优化

#### 优化1：返回引用而非拷贝
```cpp
// 改进前
Token consume(TokenType type) {
    if (check(type)) return tokens[current++];
    throw runtime_error("Parse error");
}

// 改进后
const Token& consume(TokenType type) {
    if (check(type)) return tokens[current++];
    throw runtime_error("Parse error");
}
```

#### 优化2：对象池减少分配
```cpp
// 使用内存池分配 AST 节点
class ASTNodePool {
    vector<unique_ptr<ASTNode>> nodes;
public:
    template<typename T, typename... Args>
    T* create(Args&&... args) {
        auto node = make_unique<T>(forward<Args>(args)...);
        T* ptr = node.get();
        nodes.push_back(move(node));
        return ptr;
    }
};
```

---

### 2.3 代码生成器优化（重点）

#### 优化1：使用虚函数替代 dynamic_cast

**改进前：**
```cpp
void genExpr(Expr* expr) {
    if (auto* num = dynamic_cast<NumberExpr*>(expr)) {...}
    if (auto* ident = dynamic_cast<IdentExpr*>(expr)) {...}
    if (auto* unary = dynamic_cast<UnaryExpr*>(expr)) {...}
    // ...
}
```

**改进后（访问者模式）：**
```cpp
// 表达式基类添加虚函数
struct Expr : ASTNode {
    virtual void accept(ExprVisitor& v) = 0;
};

struct NumberExpr : Expr {
    int value;
    void accept(ExprVisitor& v) override { v.visit(*this); }
};

// 代码生成器实现访问者
class CodeGenVisitor : public ExprVisitor {
    void visit(NumberExpr& e) override { emit("li t0, " + to_string(e.value)); }
    void visit(IdentExpr& e) override { /* 加载变量 */ }
    // ...
};
```

**收益**: 避免 RTTI 开销，使用虚函数表直接分发

#### 优化2：枚举类型判断（更简单的方案）

**改进后：**
```cpp
enum class ExprKind { NUMBER, IDENT, BINARY, UNARY, CALL };

struct Expr : ASTNode {
    ExprKind kind;
    Expr(ExprKind k) : kind(k) {}
};

void genExpr(Expr* expr) {
    switch (expr->kind) {
        case ExprKind::NUMBER: {
            auto* num = static_cast<NumberExpr*>(expr);
            emit("li t0, " + to_string(num->value));
            break;
        }
        case ExprKind::IDENT: {
            auto* ident = static_cast<IdentExpr*>(expr);
            // ...
            break;
        }
        // ...
    }
}
```

**收益**: switch 比多次 dynamic_cast 快得多

#### 优化3：多寄存器利用

**改进前：**
```cpp
// 每次二元运算都压栈保存
genExpr(binary->left.get());
emit("addi sp, sp, -4");
emit("sw t0, 0(sp)");
genExpr(binary->right.get());
emit("mv t1, t0");
emit("lw t0, 0(sp)");
emit("addi sp, sp, 4");
```

**改进后（寄存器栈）：**
```cpp
class CodeGenerator {
    int regIndex = 0;  // 当前使用的寄存器索引
    const char* regs[7] = {"t0", "t1", "t2", "t3", "t4", "t5", "t6"};

    const char* allocReg() {
        if (regIndex < 7) return regs[regIndex++];
        // 溢出到栈...
    }

    void freeReg() { if (regIndex > 0) regIndex--; }

    void genBinaryExpr(BinaryExpr* binary, const char* destReg) {
        const char* leftReg = allocReg();
        genExpr(binary->left.get(), leftReg);
        const char* rightReg = allocReg();
        genExpr(binary->right.get(), rightReg);

        emit(string("add ") + destReg + ", " + leftReg + ", " + rightReg);

        freeReg();  // 释放 rightReg
        freeReg();  // 释放 leftReg
    }
};
```

**收益**: 减少栈操作指令，生成更紧凑的代码

#### 优化4：常量折叠

在代码生成前进行编译期计算：

```cpp
unique_ptr<Expr> foldConstants(Expr* expr) {
    if (auto* binary = dynamic_cast<BinaryExpr*>(expr)) {
        auto left = foldConstants(binary->left.get());
        auto right = foldConstants(binary->right.get());

        auto* leftNum = dynamic_cast<NumberExpr*>(left.get());
        auto* rightNum = dynamic_cast<NumberExpr*>(right.get());

        if (leftNum && rightNum) {
            int result;
            if (binary->op == "+") result = leftNum->value + rightNum->value;
            else if (binary->op == "-") result = leftNum->value - rightNum->value;
            else if (binary->op == "*") result = leftNum->value * rightNum->value;
            else if (binary->op == "/") result = leftNum->value / rightNum->value;
            // ...
            return make_unique<NumberExpr>(result);
        }
    }
    return nullptr;  // 保持原表达式
}
```

**收益**: 减少运行时计算，生成更少的指令

#### 优化5：字符串输出优化

```cpp
// 改进前
void emit(const string& s) { out << "\t" << s << "\n"; }
emit("add t0, t0, t1");  // 运行时拼接

// 改进后：预定义指令模板
void emitAdd(const char* rd, const char* rs1, const char* rs2) {
    out << "\tadd " << rd << ", " << rs1 << ", " << rs2 << "\n";
}

// 或使用 fmt 库
void emit(const char* fmt, ...) { /* 格式化输出 */ }
```

---

### 2.4 编译选项优化

编译 `compiler.cpp` 时使用优化标志：

```bash
# 推荐编译命令
g++ -O3 -march=native -flto compiler.cpp -o compiler.exe

# 各选项说明
# -O3: 最高级别优化
# -march=native: 针对本机 CPU 优化
# -flto: 链接时优化
```

---

## 三、优化优先级建议

| 优先级 | 优化项 | 预计收益 | 实现难度 |
|--------|--------|----------|----------|
| 1 | 使用 -O3 编译 | 高 | 极低 |
| 2 | 枚举替代 dynamic_cast | 高 | 低 |
| 3 | 静态关键字表 | 中 | 极低 |
| 4 | 多寄存器利用 | 中 | 中 |
| 5 | 常量折叠 | 中 | 中 |
| 6 | Token 返回引用 | 低 | 极低 |
| 7 | 访问者模式重构 | 中 | 高 |

---

## 四、快速实施方案（推荐）

如果时间有限，建议优先实施以下 3 项改动：

### 4.1 静态关键字表（5分钟）

修改 `tokenize()` 函数：
```cpp
vector<Token> tokenize() {
    // 改为 static const unordered_map
    static const unordered_map<string, TokenType> keywords = {
        {"int", TokenType::INT}, {"void", TokenType::VOID},
        {"if", TokenType::IF}, {"else", TokenType::ELSE},
        {"while", TokenType::WHILE}, {"break", TokenType::BREAK},
        {"continue", TokenType::CONTINUE}, {"return", TokenType::RETURN}
    };
    // ... 其余不变
}
```

### 4.2 添加节点类型枚举（15分钟）

```cpp
enum class ExprKind { NUMBER, IDENT, BINARY, UNARY, CALL };
enum class StmtKind { BLOCK, VARDECL, ASSIGN, IF, WHILE, BREAK, CONTINUE, RETURN, EXPR, EMPTY };

struct Expr : ASTNode {
    ExprKind kind;
    Expr(ExprKind k) : kind(k) {}
};

struct Stmt : ASTNode {
    StmtKind kind;
    Stmt(StmtKind k) : kind(k) {}
};

// 各子类添加构造函数
struct NumberExpr : Expr {
    int value;
    NumberExpr(int v) : Expr(ExprKind::NUMBER), value(v) {}
};
// ... 其他类似
```

然后在 `genExpr()` 和 `genStmt()` 中使用 `switch` 替代 `dynamic_cast`。

### 4.3 使用 -O3 编译（1分钟）

```bash
g++ -O3 compiler.cpp -o compiler.exe
```

---

## 五、性能测试方法

优化前后对比测试：

```bash
# 生成测试文件（大量代码）
python generate_test.py > big_test.c

# 测量编译时间 (Windows)
powershell -Command "Measure-Command { ./compiler.exe < big_test.c > output.s }"

# 或使用 Linux
time ./compiler < big_test.c > output.s
```

预期优化后编译速度提升 **2-5 倍**（取决于输入规模）。
